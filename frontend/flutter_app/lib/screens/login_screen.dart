import 'package:flutter/material.dart';

/// Pantalla de login de OhMyFreezer.
///
/// Stub temporal — el diseño e implementación completos se realizan en la Capa 5.
/// Incluirá: formulario usuario/contraseña, validación, llamada a
/// [AuthService] y navegación a [HomeScreen] según el rol del usuario.
class LoginScreen extends StatelessWidget {
  const LoginScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Login — próximamente'),
      ),
    );
  }
}